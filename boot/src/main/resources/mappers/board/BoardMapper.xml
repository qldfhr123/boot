<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.care.boot.board.BoardMapper">
	<resultMap type="com.care.boot.board.BoardDTO" id="boardDto">
		<id column="no" property="no"/>
		<result column="title" property="title"/>
		<result column="content" property="content"/>
		<result column="id" property="id"/>
		<result column="write_date" property="writeDate"/>
		<result column="hits" property="hits"/>
		<result column="file_name" property="fileName"/>
	</resultMap>

	<select id="count">
	  	SELECT count(no) FROM boot_board
	</select>
  
	<select id="boardForm" resultMap="boardDto">
		SELECT BBB.* FROM 
		(SELECT ROWNUM() as rn, AAA.* FROM 
		(SELECT no, title, id, write_date, hits FROM boot_board ORDER BY no DESC)AAA)BBB
		WHERE BBB.rn BETWEEN #{begin} AND #{end}
	</select>
	
	<insert id="boardWriteProc">
		<selectKey order="BEFORE" keyProperty="no" resultType="int">
			SELECT nvl(max(no),0) + 1 FROM boot_board
		</selectKey>
		INSERT INTO boot_board VALUES(#{no}, #{title}, #{content}, #{id}, #{writeDate}, 0, #{fileName} )
	</insert>
	
	<select id="boardContent" resultMap="boardDto">
		SELECT * FROM boot_board WHERE no=#{no}
	</select>
	
	<update id="incHit">
		UPDATE boot_board SET hits=hits+1 WHERE no=#{no}
	</update>
	
	<select id="boardDownload">
		SELECT file_name FROM session_board WHERE no=#{no}
	</select>
	
	<update id="boardModifyProc">
		UPDATE boot_board SET title=#{title}, content=#{content} WHERE no=#{no}
	</update>
	
	<delete id="boardDeleteProc">	
		DELETE FROM boot_board WHERE no=#{no}
	</delete>
</mapper>









